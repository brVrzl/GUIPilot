name: Conda Environment Matrix Check

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  env-setup:
    name: Setup ${{ matrix.platform.name }}
    runs-on: ${{ matrix.platform.runner }}
    defaults:
      run:
        shell: bash -l {0}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - name: macos
            runner: macos-14
            setup_args: --platform macos --name guipilot-ci
            smoke_env: guipilot-ci
          - name: linux-gpu
            runner: ubuntu-latest
            setup_args: --platform linux-gpu --name guipilot-ci
            smoke_env: guipilot-ci
          - name: windows
            runner: windows-latest
            setup_args: --platform windows --name guipilot-ci
            smoke_env: guipilot-ci

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          miniforge-version: latest
          python-version: "3.10"
          channels: conda-forge,defaults
          use-mamba: true

      - name: conda info
        run: conda info

      - name: Create/Update environment
        run: python scripts/setup_env.py ${{ matrix.platform.setup_args }}

      - name: Smoke test environment
        run: >
          conda run -n ${{ matrix.platform.smoke_env }}
          python -c "import sys; import platform; print(sys.version); print(platform.platform())"

      - name: Remove CI environment
        if: ${{ always() }}
        run: conda env remove -n ${{ matrix.platform.smoke_env }} || true

